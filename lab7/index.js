var video = document.createElement("video");
    video.src = "video.mp4";
    video.width = 640;
    video.height = 360;
    video.controls = true;
    document.getElementById("videoContainer").appendChild(video);